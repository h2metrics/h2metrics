model H2.mod;
data H2.dat;
option solver ipopt;
option ipopt_options "max_iter=1000";
table H2 OUT:{t in KT,p in KP} -> 
  [KT,KP],
  d[t,p,1] ~ dx,
  d[t,p,2] ~ dy
;
solve;
write table H2;

display(energy);

